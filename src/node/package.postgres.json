{
	"name": "@w-kuipers/alphadb-postgres",
	"version": "1.0.0",
	"description": "AlphaDB is a versatile tool for managing PostgreSQL database versions, using JSON to define structures and simplify migrations across environments.",
	"author": "Wibo Kuipers",
	"main": "./lib/index.cjs",
	"license": "GPL-3.0",
	"scripts": {
		"test": "npm run test:postgres",
		"test:engines": "npm run test:mysql && npm run test:postgres",
		"test:mysql": "npm run cargo-build -- --features mysql && ALPHADB_ENGINE=mysql vitest run",
		"test:postgres": "npm run cargo-build -- --no-default-features --features postgres && ALPHADB_ENGINE=postgres vitest run",
		"cargo-build": "tsc &&cargo build --message-format=json-render-diagnostics > cargo.log",
		"cross-build": "tsc &&cross build --message-format=json-render-diagnostics > cross.log",
		"postcargo-build": "neon dist < cargo.log",
		"postcross-build": "neon dist -m /target < cross.log",
		"debug": "npm run cargo-build -- --no-default-features --features postgres",
		"build": "npm run build:postgres",
		"build:mysql": "npm run cargo-build -- --release --features mysql",
		"build:postgres": "npm run cargo-build -- --release --no-default-features --features postgres",
		"cross": "npm run cross:postgres",
		"cross:mysql": "npm run cross-build -- --release --features mysql",
		"cross:postgres": "npm run cross-build -- --release --no-default-features --features postgres",
		"postinstall": "node ./postinstall.mjs"
	},
	"exports": {
		".": {
			"import": {
				"types": "./lib/index.d.mts",
				"default": "./lib/index.mjs"
			},
			"require": {
				"types": "./lib/index.d.cts",
				"default": "./lib/index.cjs"
			}
		}
	},
	"types": "./lib/index.d.cts",
	"files": [
		"lib/**/*.?({c,m}){t,j}s",
		"crates/**/*",
		"postinstall.mjs",
		"Cargo.toml"
	],
	"neon": {
		"type": "library",
		"org": "@alphadb-postgres",
		"platforms": "common",
		"load": "./src/load.cts"
	},
	"devDependencies": {
		"@tsconfig/node20": "^20.1.4",
		"@types/node": "^20.11.16",
		"typescript": "^5.3.3",
		"vitest": "^2.1.8"
	},
	"dependencies": {
		"@neon-rs/cli": "^0.1.82",
		"@neon-rs/load": "^0.1.82",
		"node-fetch": "^3.3.2"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/w-kuipers/alphadb.git"
	},
	"keywords": [
		"database",
		"rust",
		"extension",
		"postgres database management",
		"postgres version management",
		"database migration tool",
		"database version management"
	],
	"bugs": {
		"url": "https://github.com/w-kuipers/alphadb/issues"
	},
	"homepage": "https://alphadb.w-kuipers.com"
}
